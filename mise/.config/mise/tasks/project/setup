#!/usr/bin/env bash
#MISE description="Install project dependencies from the first supported lockfile"
#MISE dir="{{cwd}}"
set -Eeuo pipefail

task_file=${MISE_TASK_FILE:-$HOME/.config/mise/tasks/project/setup}
config_root=${task_file%%/tasks/*}
exec "$config_root/scripts/project.sh" setup "$@"
