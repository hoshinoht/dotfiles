#!/usr/bin/env bash
#MISE description="Validate project metadata without changing lockfiles"
#MISE dir="{{cwd}}"
set -Eeuo pipefail

task_file=${MISE_TASK_FILE:-$HOME/.config/mise/tasks/project/check}
config_root=${task_file%%/tasks/*}
exec "$config_root/scripts/project.sh" check "$@"
