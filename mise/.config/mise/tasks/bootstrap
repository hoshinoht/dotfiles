#!/usr/bin/env bash
#MISE description="Install configured mise tools and the default uv-managed Python"
#MISE dir="{{cwd}}"
set -Eeuo pipefail

task_file=${MISE_TASK_FILE:-$HOME/.config/mise/tasks/bootstrap}
config_root=${task_file%%/tasks/*}
exec "$config_root/scripts/runtime.sh" bootstrap "$@"
