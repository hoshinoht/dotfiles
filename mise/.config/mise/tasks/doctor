#!/usr/bin/env bash
#MISE description="Diagnose mise configuration without changing installed tools"
#MISE dir="{{cwd}}"
set -Eeuo pipefail

task_file=${MISE_TASK_FILE:-$HOME/.config/mise/tasks/doctor}
config_root=${task_file%%/tasks/*}
exec "$config_root/scripts/runtime.sh" doctor "$@"
